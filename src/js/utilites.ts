// export function rangeMap(input: number, inMin: number, inMax: number, outMin: number, outMax: number) {
//          return ((input - inMin) * (outMax - outMin)) / (inMax - inMin) + outMin;
//        }